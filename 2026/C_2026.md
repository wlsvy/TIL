**2026 / C**

# 26.01.06

## [Top 10 errors found in C projects in 2025 - DEV Community](https://dev.to/pvsdev/top-10-errors-found-in-c-projects-in-2025-10ml)

정적 분석 도구인 PVS-Studio 팀에서 발표한 **"2025년 오픈소스 C# 프로젝트에서 발견된 10가지 주요 에러"** 아티클을 정리하고 번역해 드립니다.

### 10위. 단순 복사-붙여넣기 실수 (Copy-paste)

* **오류:** `TypeCode.UInt64` 케이스에서 `ToUInt64()`가 아닌 `ToInt64()` 메서드를 호출함.
* **원인:** 유사한 코드를 반복해서 작성하다가 메서드 이름을 수정하지 않은 전형적인 복붙 에러입니다.

```cs
public static void SetAsIConvertible(this ref ComVariant variant,
                                     IConvertible value)
{
  TypeCode tc = value.GetTypeCode();
  CultureInfo ci = CultureInfo.CurrentCulture;

  switch (tc)
  {
    case TypeCode.Empty: break;
    case TypeCode.Object: 
      variant = ComVariant.CreateRaw(....); break;
    case TypeCode.DBNull: 
      variant = ComVariant.Null; break;
    case TypeCode.Boolean: 
      variant = ComVariant.Create<bool>(....)); break;
    case TypeCode.Char: 
      variant = ComVariant.Create<ushort>(value.ToChar(ci)); break;
    case TypeCode.SByte: 
      variant = ComVariant.Create<sbyte>(value.ToSByte(ci)); break;
    case TypeCode.Byte: 
      variant = ComVariant.Create<byte>(value.ToByte(ci)); break;
    case TypeCode.Int16: 
      variant = ComVariant.Create(value.ToInt16(ci)); break;
    case TypeCode.UInt16: 
      variant = ComVariant.Create(value.ToUInt16(ci)); break;
    case TypeCode.Int32: 
      variant = ComVariant.Create(value.ToInt32(ci)); break;
    case TypeCode.UInt32: 
      variant = ComVariant.Create(value.ToUInt32(ci)); break;
    case TypeCode.Int64: 
      variant = ComVariant.Create(value.ToInt64(ci)); break;
    case TypeCode.UInt64: 
      variant = ComVariant.Create(value.ToInt64(ci)); break; // <-- Here
    case TypeCode.Single: 
      variant = ComVariant.Create(value.ToSingle(ci)); break;
    case TypeCode.Double: 
      variant = ComVariant.Create(value.ToDouble(ci)); break;
    case TypeCode.Decimal: 
      variant = ComVariant.Create(value.ToDecimal(ci)); break;
    case TypeCode.DateTime: 
      variant = ComVariant.Create(value.ToDateTime(ci)); break;
    case TypeCode.String: 
      variant = ComVariant.Create(....); break;

    default:
      throw new NotSupportedException();
  }
}
```

### 9위. 잘못된 문자열 포맷팅 (AppendFormat)

* **오류:** `sb.AppendFormat("{1:X04}...", arg1, arg2...)` 호출 시 예외 발생.
* **원인:** 1.  포맷 플레이스홀더(`{3}`, `{4}`) 개수보다 전달된 인자가 적음.
2.  C# 포맷 인덱스는 **1이 아닌 0부터 시작**해야 하는데 `{1}`부터 시작함.

### 8위. Equals와 GetHashCode의 불일치 (1)

* **오류:** `GetHashCode`에서는 `Legs` 속성을 사용해 해시를 계산하지만, `Equals`에서는 이를 비교하지 않음.
* **원인:** 특히 이 사례에서는 `Dictionary`를 만들 때 자기 자신의 컬렉션을 다시 참조하여 "컬렉션의 요소가 같은 컬렉션에 있는지" 확인하는 무의미한 로직이 포함되었습니다.

```cs
public override int GetHashCode()
{
  unchecked
  {
    var hashCode = Definition.GetHashCode();
    var arr = new int[Legs.Count];
    for (int i = 0; i < Legs.Count; i++)
    {
      arr[i] = Legs[i].GetHashCode();
    }

    Array.Sort(arr);

    for (int i = 0; i < arr.Length; i++)
    {
      hashCode = (hashCode * 397) ^ arr[i];
    }

    return hashCode;
  }
}

public override bool Equals(object obj)
{
    ....

    return Equals((OptionStrategyDefinitionMatch) obj);
}
```

### 7위. 속성이 누락된 Equals 구현

* **오류:** `Equals` 메서드에서 `Min` 속성만 두 번 비교하고 `Max` 비교를 누락함.
* **분석:** `Equals(Min, other.Min) && Equals(Min, other.Min)`과 같이 작성된 코드입니다. `GetHashCode`에는 `Max`가 포함되어 있어 분석기가 불일치를 감지했습니다.

### 6위. 도달할 수 없는 비트 연산 코드 (Switch Pattern)

* **오류:** `keys & ~Keys.Modifiers` 연산 후 `Keys.Shift` 등을 체크하는 switch 문에서 특정 case가 절대 실행되지 않음.
* **원인:** `Keys.Modifiers` 비트 마스크에 `Shift`, `Control`, `Alt` 비트가 이미 포함되어 있어, 이를 제거하면 해당 값들과 일치할 수 없게 됩니다.

```cs
public static Interactivity.Key GetKey(this Keys keys)
{

  Keys keyCode = keys & ~Keys.Modifiers;                   // <=
  Interactivity.Key key = keyCode switch
  {
    Keys.Alt => Interactivity.StandardKeys.Alt,            // <=
    Keys.Menu => Interactivity.StandardKeys.Alt,
    Keys.Shift => Interactivity.StandardKeys.Shift,        // <=
    Keys.ShiftKey => Interactivity.StandardKeys.Shift,
    Keys.LShiftKey => Interactivity.StandardKeys.Shift,
    Keys.RShiftKey => Interactivity.StandardKeys.Shift,
    Keys.Control => Interactivity.StandardKeys.Control,    // <=
    Keys.ControlKey => Interactivity.StandardKeys.Control,
    Keys.Down => Interactivity.StandardKeys.Down,
    Keys.Up => Interactivity.StandardKeys.Up,
    Keys.Left => Interactivity.StandardKeys.Left,
    Keys.Right => Interactivity.StandardKeys.Right,
    _ => Interactivity.StandardKeys.Unknown,
  };

  ....
}
```

### 5위. 값 형식(Struct)의 ReferenceEquals 사용

* **오류:** `struct StackValue`의 `Equals` 메서드 내에서 `ReferenceEquals(this, obj)` 호출.
* **원인:** `this`가 값 형식이므로 `ReferenceEquals`에 전달되는 순간 **박싱(Boxing)**이 발생합니다. 힙에 새로 생성된 참조는 절대 같을 수 없으므로 이 비교는 항상 `false`를 반환합니다.

### 4위. 익명 함수를 이용한 이벤트 구독 해제 실패

* **오류:** `Event -= () => Action();` 형태의 코드.
* **원인:** 익명 함수는 선언될 때마다 **새로운 델리게이트 인스턴스**를 생성합니다. 구독할 때의 인스턴스와 해제할 때의 인스턴스가 다르므로 구독 해제가 작동하지 않습니다.

```cs
private static void SubscribeImmutablePathsInitialized()
{
  NotifyOnScopingReadiness?.Invoke();

  FileClassifier.Shared.OnImmutablePathsInitialized -= () =>
    NotifyOnScopingReadiness?.Invoke();
}
```

### 3위. 연산자 우선순위 혼동 (?? vs +)

* **오류:** `base.Size + nullableObj?.Size ?? 0`
* **원인:** `+` 연산자가 `??`보다 우선순위가 높습니다. 따라서 `(base.Size + null) ?? 0`이 되어 왼쪽 항이 `null`이면 결과가 항상 `0`이 됩니다. 의도한 대로라면 괄호를 써서 `base.Size + (nullableObj?.Size ?? 0)`이 되어야 합니다.

```cs
public override int Size =>   base.Size
                            + ChangeViewMessages?.Values.GetVarSize() ?? 0
                            + 1 + PrepareRequestMessage?.Size ?? 0
                            + PreparationHash?.Size ?? 0
                            + PreparationMessages?.Values.GetVarSize() ?? 0
                            + CommitMessages?.Values.GetVarSize() ?? 0;
```

### 2위. 논리 패턴 매칭의 함정 (not 0 or 1)

* **오류:** `itemCount is not 0 or 1`
* **원인:** 개발자는 "0도 아니고 1도 아님"을 의도했겠지만, C# 컴파일러는 이를 **`(is not 0) or (is 1)`**로 해석합니다. 1인 경우에도 참이 되는 논리적 오류입니다. 정확하게는 `is not (0 or 1)`로 써야 합니다.

```cs
protected void ChangeMode(OmnibarMode? oldMode, OmnibarMode newMode)
{
  ....
  var modeSeparatorWidth = 
    itemCount is not 0 or 1 
      ? _modesHostGrid.Children[1] is FrameworkElement frameworkElement
        ? frameworkElement.ActualWidth
        : 0
      : 0;
  ....
}
```

### 1위. LINQ 지연 실행과 변수 캡처의 결합

* **오류:** `Select` 내부에서 외부 변수 `lineNumber++`를 수행하고, 이후 `foreach` 루프에서도 동일한 변수를 `++` 함.
* **원인:** `Select`는 **지연 실행(Deferred Execution)** 메서드입니다. 실제 값이 계산되는 시점은 `foreach` 문이 돌 때입니다. 결과적으로 루프 한 번에 변수가 두 번씩 증가하거나, 초기화 시점이 꼬여 예상치 못한 값을 가지게 됩니다.

```cs
public void FutureMarginModel_MarginEntriesValid(string market)
{
  ....
  var lineNumber = 0;
  var errorMessageTemplate = $"Error encountered in file " + 
                             $"{marginFile.Name} on line ";
  var csv = File.ReadLines(marginFile.FullName)
                .Where(x =>    !x.StartsWithInvariant("#") 
                            && !string.IsNullOrWhiteSpace(x))
                .Skip(1)
                .Select(x =>
  {
    lineNumber++;                                                  // <=

    ....
  });

  lineNumber = 0;                                                  // <=
  foreach (var line in csv)
  {
    lineNumber++;                                                  // <=

    ....
  }
}
```

# 26.01.08

## [IO is no longer the bottleneck](https://stoppels.ch/2022/11/27/io-is-no-longer-the-bottleneck.html)

Ben Hoyt's blog refers to an [earlier post](https://benhoyt.com/writings/count-words/) which includes a faster C version of the word frequency counter. I compiled optimized.c with GCC 12, using -O3 -march=native flags, and ran it on the 425MB input file (100 copies of the King James Version of the Bible).

이 글은 "현대 시스템에서 데이터 처리의 병목은 디스크 읽기(I/O)가 아니라 CPU 성능"이라는 주장을 검증하기 위해, 단어 수 세기(Word Count) 프로그램을 극한으로 최적화해 본 과정을 담고 있습니다. (2022. 11. 27)

### 1. 배경: 현대 디스크의 속도

최근 벤 호이트(Ben Hoyt)는 "순차 읽기(Sequential Read) 속도는 엄청나게 빨라진 반면, CPU 속도는 정체되었기 때문에 I/O가 병목이라는 믿음은 더 이상 사실이 아니다"라고 주장했습니다.

* **저자의 측정 결과:** * **Cold Cache(디스크에서 직접 읽기):** 약 1.6 GB/s
* **Warm Cache(메모리에 로드된 상태):** 약 12.8 GB/s

### 2. 최적화된 C 코드의 한계

벤 호이트가 작성한 최적화된 C 언어 단어 빈도 계산 코드를 테스트해 본 결과, 웜 캐시 상태에서도 **278 MB/s**라는 실망스러운 속도가 나왔습니다. 디스크 읽기 속도(1.6 GB/s)에 한참 못 미치는 수치입니다.

* **원인 분석:** 루프 내에 많은 분기문(Branch)이 있어 컴파일러의 **자동 벡터화(Auto-vectorization)**를 방해하고 있었습니다.
* **개선 시도:** 소문자 변환 로직을 루프 밖으로 빼는 등 약간의 수정을 거쳐 **330 MB/s**까지 끌어올렸으나, 여전히 디스크 속도보다 5배나 느렸습니다.

### 3. 리눅스 기본 도구 `wc -w`의 성능

가장 단순한 작업인 단어 수 세기 전용 도구 `wc`는 어떨까요?

* **결과:** **245.2 MB/s**로 생각보다 매우 느렸습니다.
* **이유:** `wc`는 단순 공백(' ')뿐만 아니라 탭, 줄 바꿈, 로케일별 특수 문자 등을 모두 처리해야 하므로 범용적인 대신 느립니다.

### 4. 극한의 최적화: AVX2 벡터화

저자는 지난 10년간 비약적으로 발전한 CPU의 기능을 활용하기 위해 **SIMD(AVX2)**를 직접 사용하여 코드를 작성하기로 했습니다.

* **핵심 기술:**
1. **256비트 레지스터:** 한 번에 32개의 문자를 처리.
2. **VPCMPEQB:** 레지스터 내에서 공백 문자를 찾아 마스크(Mask)를 생성.
3. **PMOVMSKB & 비트 트릭:** 생성된 마스크를 32비트 정수로 변환한 뒤, 비트 연산(`ffs` 등)을 통해 단어의 시작과 끝을 빠르게 계산.
4. **루프 언롤링(Unrolling):** 루프를 4배로 풀어 한 번에 128바이트의 데이터를 처리.

- 결과적으로 Warm Cache 기준 **1.45 GB/s** CPU가 거의 100% 사용됨

### ### 결론

* **I/O는 더 이상 병목이 아니다:** 단일 스레드에서 가장 빠른 벡터화 명령어를 사용하여 단어 수만 세는 단순 작업을 수행하더라도, 겨우 **Cold Cache 디스크 읽기 속도(1.6 GB/s)의 약 90%** 정도만 따라잡을 수 있었습니다.
* **웜 캐시 기준:** CPU 처리 속도(1.45 GB/s)는 메모리에 올라온 데이터 읽기 속도(12.8 GB/s)의 **약 11%** 수준에 불과합니다.
* **시사점:** 이제 하드웨어(NVMe SSD 등)의 발전 속도가 소프트웨어와 CPU의 처리 능력을 앞질렀습니다. 복잡한 로직이 들어가는 프로그램이라면 병목은 십중팔구 I/O가 아닌 CPU에서 발생할 것입니다.

# 26.01.21

## [Devs Discuss Updating Backend Code from .NET 8 to .NET 10 'Like a Breeze' -- Visual Studio Magazine](https://visualstudiomagazine.com/articles/2026/01/05/devs-discuss-updating-backend-code-from-net-8-to-net-10-like-a-breeze.aspx)

전달해주신 **.NET 8에서 .NET 10으로의 백엔드 코드 마이그레이션**에 관한 기사 내용을 핵심 위주로 정리하여 번역해 드립니다.

---

# .NET 8 → .NET 10 백엔드 업데이트: "식은 죽 먹기(Like a Breeze)"

 - 2026년 1월 5일 by David Ramel

* **간편한 업그레이드:** 대부분의 개발자는 타겟 프레임워크(Target Framework) 설정을 바꾸는 것만으로 업그레이드가 완료되었다고 평가함.
* **제한적인 파괴적 변경:** 호환성이 깨지는 변경 사항(Breaking Changes)은 드물며, 주로 특정 라이브러리, 툴링, OpenAPI 통합과 관련됨.
* **확실한 보상:** 성능 향상과 장기 지원(LTS) 혜택만으로도 업그레이드할 가치가 충분함.

기사 속 개발자들은 마이크로소프트의 최근 행보에 회의적이었던 이들조차 **".NET 10 마이그레이션은 그럴 가치가 충분하다"**고 평가하고 있습니다. 특별한 신규 기능을 사용하지 않더라도 성능과 보안 측면에서 즉각적인 이득을 얻을 수 있기 때문입니다.

### 🛠 주요 파괴적 변경 사항 (Breaking Changes)

일부 이슈가 보고되었으나, 대부분 특정 종속성이나 도구에 국한된 문제로 파악되었습니다.

| 항목                  | 상세 내용                                                                                                                     |
| ---                   | ---                                                                                                                           |
| **System.Linq.Async** | 기본 제공되는 `AsyncEnumerable` 지원으로 대체됨. 외부 패키지를 제거하고 `SelectAwait` 등의 메서드 사용 시 코드 수정이 필요함. |
| **EF Core 10**        | 동적 조건부 벌크 삭제(Dynamic conditional bulk delete) 방식의 변경이 보고됨.                                                  |
| **Source Generators** | 비증분(Non-incremental) 소스 제너레이터가 폐지됨. 증분 소스 제너레이터로 재작성 필수.                                         |
| **OpenAPI (Swagger)** | 기본 생성 버전이 **OpenAPI 3.1.1**로 변경됨. Azure API Management 등 3.0.x를 사용하는 기존 CI/CD 환경에서 충돌 발생 가능.     |
| **의존성 생태계**     | PostgreSQL 및 MySQL(Pomelo)용 EF Core 프로바이더가 .NET 10을 지원할 때까지 업그레이드가 지연될 수 있음.                       |
| **툴링 (Tooling)**    | .NET 10을 타겟팅하기 위해 최신 버전의 Visual Studio 설치가 필요함.                                                            |

# 26.02.02

## [개발자들이 맥 쓰는 3가지 이유 (비싼데도) - YouTube](https://www.youtube.com/watch?v=nkWdveHFrVI)

이 영상은 단순히 디자인이나 배터리 효율 때문이 아니라, 개발자들이 맥(macOS)을 선호할 수밖에 없는 **운영체제(OS)의 아키텍처적 본질**에 대해 설명하고 있습니다.

**1. 완성도 높은 '네이티브 유닉스(Unix)' 환경**

* **서버와의 일관성:** macOS는 태생이 유닉스 기반입니다. 따라서 개발자들이 실제로 서비스를 운영하는 리눅스 서버와 시스템 핵심 동작 방식이 거의 같습니다 `[00:00:21]`.
* **네이티브 아키텍처:** 윈도우는 WSL(Windows Subsystem for Linux)을 통해 격차를 줄이려 하지만, 이는 결국 호환성을 위한 '레이어'일 뿐입니다. 반면 맥은 시스템 코어부터 유닉스 도구와 동작 방식을 네이티브로 갖추고 있어 훨씬 강력하고 일관된 개발 환경을 제공합니다 `[00:00:44]`.

**2. 모듈화된 설정 관리 체계 (레지스트리 없음)**

* **윈도우의 문제점:** 윈도우는 '레지스트리'라는 중앙 저장소에 모든 설정을 몰아넣습니다. 시간이 지날수록 이 레지스트리가 비대해지고 꼬이면서 시스템이 느려지거나 불안정해지는 고질적인 문제가 발생합니다 `[00:01:21]`.
* **맥의 방식:** macOS는 레지스트리를 사용하지 않습니다. 대신 앱별, 사용자별로 모듈화된 **plist 파일**에 설정을 저장합니다 `[00:01:46]`. 덕분에 특정 앱의 설정만 디버깅하거나 초기화하기가 매우 쉽고, 수년을 써도 시스템 성능이 처음처럼 쾌적하게 유지됩니다 `[00:01:58]`.

**3. 커널 레벨의 강력한 보안 (시스템 무결성 보호, SIP)**

* **SIP (System Integrity Protection):** 맥은 관리자 권한을 가진 사용자나 앱이라 하더라도 시스템 핵심 파일과 프로세스를 함부로 수정하지 못하도록 커널 수준에서 차단합니다 `[00:02:34]`.
* **보안의 본질적 차이:** 윈도우는 악성코드가 관리자 권한을 탈취하면 시스템 전체가 위협받지만, 맥은 OS 아키텍처 수준에서 방어 구역을 설정해 두어 악성코드 확산이 어렵고 훨씬 안전한 기반 위에서 동작합니다 `[00:02:46]`.

# 26.02.20

## [요즘 핵쟁이가 멸종 중인 이유](https://www.youtube.com/watch?v=Cm2M3bbRoKA)

- [뱅가드가 무엇인가요? – VALORANT 고객지원](https://support-valorant.riotgames.com/hc/ko/articles/360046160933-%EB%B1%85%EA%B0%80%EB%93%9C%EA%B0%80-%EB%AC%B4%EC%97%87%EC%9D%B8%EA%B0%80%EC%9A%94)

기술적 방어 체계가 고도화되고 핵 사용에 필요한 비용과 리스크가 감당하기 어려운 수준으로 높아지면서 핵 유저가 감소하고 있는 추세입니다. 다만, 일반 유저들도 보안상의 이유로 커널 레벨 프로그램을 항상 구동해야 하는 부담을 안게 되었다는 점도 언급하고 있습니다 [[04:23](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=263)].

### **1. 핵 감지 기술의 발전 (커널 레벨 안티치트)**

* 과거의 핵은 주로 메모리 조작이나 DLL 파일 삽입 방식이었으나, 최근에는 게임사들이 시스템의 가장 깊은 영역인 **커널 레벨**에서 작동하는 안티치트 프로그램(예: 라이엇의 뱅가드)을 도입했습니다 [[01:01](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=61)].
* 이러한 프로그램은 부팅 시 드라이버 형태로 먼저 로드되어 수상한 드라이버나 외부 장치의 접근을 사전에 차단합니다 [[02:26](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=146)].

### **2. 하드웨어 보안 강화**

* 최근에는 **TPM**이나 **시큐어 부트(Secure Boot)** 설정을 강제하여, 부팅 과정에서 변조된 프로그램이 실행되는 것을 막고 있습니다 [[02:42](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=162)].
* 심지어 하드웨어 보안 취약점이 있는 특정 메인보드 제조사에 패치를 요구하는 등 하드웨어 단계에서의 보안도 강화되고 있습니다 [[03:04](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=184)].

### **3. 데이터 및 패턴 분석 (AI/딥러닝)**

* 서버 측에서 유저의 플레이 패턴(반응 속도, 명중률 등)을 분석하여 통계적으로 의심스러운 '아웃라이어'를 찾아내 핵 사용 여부를 판별합니다 [[03:14](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=194)].

### **4. 핵 사용 비용의 급증 (경제적 장벽)**

* 게임사의 방어를 뚫기 위해 별도의 하드웨어 장비와 서브 컴퓨터가 필요해지면서, 핵을 사용하기 위해 **최소 100만 원 이상의 비용**이 들게 되었습니다 [[00:32](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=32)].
* 또한, 걸렸을 때 하드웨어 밴(Hardware Ban)을 당하면 CPU나 메인보드를 교체해야 하는 리스크도 커졌습니다 [[03:51](http://www.youtube.com/watch?v=Cm2M3bbRoKA&t=231)].

Q. 이 방식은 맥이나 리눅스 환경에서 지원? 발로란트는 맥 / 리눅스 플레이 가능?

**발로란트는 현재 맥(macOS)과 리눅스(Linux) 환경에서 공식적으로 플레이할 수 없습니다.**

### **왜 맥과 리눅스에서 안 되나요?**

* **커널 아키텍처의 차이:** 뱅가드는 **윈도우 커널(Windows NT Kernel)**의 드라이버 모델에 맞춰 최적화되어 개발되었습니다. 맥은 유닉스(UNIX) 기반의 **Darwin 커널**, 리눅스는 **Linux 커널**을 사용하기 때문에 시스템 호출 방식이나 보안 구조가 완전히 다릅니다. 윈도우용으로 만든 커널 드라이버를 이들 OS에 이식하는 것은 사실상 소프트웨어를 처음부터 다시 만드는 것과 같습니다.
* **보안 철학의 충돌:** 특히 리눅스는 오픈 소스이며 사용자가 커널을 자유롭게 수정할 수 있습니다. 뱅가드처럼 "시스템의 통제권을 먼저 쥐고 감시하겠다"는 방식이 리눅스의 개방적인 보안 구조와 정면으로 충돌하며, 보안 신뢰 체계(Trust Chain)를 구축하기가 매우 어렵습니다.

### **리그 오브 레전드(LoL)는 되는데 왜 발로란트는 안 되나요?**

최근 리그 오브 레전드에도 뱅가드가 도입되었지만, 맥 버전 LoL은 뱅가드 없이 별도의 보안 체계를 적용하여 서비스를 유지하고 있습니다. 하지만 **발로란트**는 게임 설계 초기부터 뱅가드와 한 몸으로 기획된 FPS 게임이기 때문에, 안티치트가 우회될 가능성이 있는 환경(맥/리눅스)은 아예 차단하고 있는 것입니다.

# 26.02.28

## [[안내] 트릭컬 노트 비공식 프로그램 배포 관련 주의사항 안내 - 트릭컬 RE:VIVE 채널](https://arca.live/b/trickcal/163502330?mode=best)

- 트릭컬 노트 웹 서비스 : 게임 유저 정보 DB 를 조회, 실용적인 포맷으로 제공
- 역시 웹이라 그런가 바로 악용 사례 등장, 유저 정보를 스크래이핑 해가는 통로로 사용하는 듯

안녕하세요, 교주님들.
에피드게임즈입니다.

먼저, 트릭컬 리바이브 IP를 바탕으로 다양한 창작 활동을 이어가 주시는 모든 분들께 깊이 감사드립니다.
보내주시는 관심과 열정은 게임의 큰 힘이 되고 있습니다.

다만, 트릭컬 노트 BETA 공개 이후 공식 웹 페이지의 정보를
비정상적인 방식으로 취득해 제작된 비공식 프로그램이 확인되고 있습니다.

이에 교주님들의 개인정보 보호와 안전한 이용 환경을 위해
해당 프로그램의 다운로드 및 사용을 지양해 주시기를 권고드립니다.

공식 웹을 통해 제공되는 트릭컬 노트 BETA 내 정보를 승인 없이 수집/가공해 앱 형태로 배포하는 행위는
「부정경쟁방지 및 영업비밀보호에 관한 법률」 및 「정보통신망 이용촉진 및 정보보호 등에 관한 법률」 위반에 해당할 소지가 있습니다.

또한 향후 트릭컬 노트에 추가될 기능까지 고려할 경우
비공식 앱에 권한을 부여하는 과정에서 이용자의 개인정보가 유출되어 안전하게 보호되지 않을 위험이 존재합니다.

특히 교주님의 게임 정보를 기반으로 데이터가 노출되는 형태의 프로그램을
제작/배포 중인 경우 해당 프로그램의 배포를 중단해 주시기 바랍니다.

현재 확인된 비공식 프로그램은 저작권법 및 체결된 라이선스 계약 범위를 침해할 가능성이 있으며
당사뿐 아니라 협업사 및 퍼블리셔의 권리에도 영향을 미칠 수 있는 사안입니다.

이에 교주님들의 안전한 게임 이용을 위해 개발사 입장에서도 취할 수 있는 모든 조치를 준비하고 있습니다.

창작 활동을 희망하시는 경우 공식 트릭컬 노트 BETA의 구조나 데이터를 활용하는 방식이 아닌
별도의 독립적인 프로그램으로 제작하시거나 2차 창작 가이드라인 범위 내에서 진행해 주시기 바랍니다.

2차 창작 가이드라인 : https://epidgames.oqupie.com/portal/2399/article/73315

더불어, 비공식 프로그램 사용으로 인해 발생하는 개인정보 유출 등
피해에 대해서는 안타깝게도 당사의 책임 범위를 벗어나는 사항입니다.

트릭컬 노트 BETA는 지속적인 개선을 통해 기능을 확장할 예정이며
향후 게임 내에서도 편리하게 이용하실 수 있도록 지원할 계획입니다.

안전하고 공식적인 경로를 통해 트릭컬 노트를 이용해 주시기 바랍니다.
감사합니다.

## [nfd2nfc/docs/README.ko.md at main · elgar328/nfd2nfc · GitHub](https://github.com/elgar328/nfd2nfc/blob/main/docs/README.ko.md)

NFD 파일명을 실시간으로 감지해 NFC로 변환하는 macOS 도구입니다. 다른 OS와 파일을 주고받을 때 생기는 파일명 깨짐 문제를 해결합니다.

유니코드에는 한글, 악센트 문자(é, ü, ñ), 일본어(が, ぱ) 같은 조합 문자를 표현하는 두 가지 정규화 방식이 있습니다:

- **NFC(Normal Form Composed)** (조합형): 하나의 코드포인트 — `가` = `U+AC00`
- **NFD(Normal Form Decomposed)** (분해형): 기본 문자 + 결합 문자 — `가` = `U+1100 U+1161`

macOS는 파일명을 NFD로 저장하지만, Windows와 Linux는 NFC를 사용합니다. 이 차이 때문에 다른 OS와 파일을 주고받으면 다음과 같은 문제가 생깁니다:

- 한글이 자모로 풀어져서 표시됨 (예: 한글 → ㅎㅏㄴㄱㅡㄹ)
- 악센트 문자가 잘못 표시되거나 정렬이 깨짐
- Git이 같은 파일을 변경된 것으로 인식
- 클라우드 동기화나 압축 해제 시 경로를 찾지 못함

**nfd2nfc**는 TUI와 백그라운드 감시 서비스를 통해 이 문제를 자동으로 해결합니다.

## [Tests Are The New Moat | Daniel Saewitz](https://saewitz.com/tests-are-the-new-moat)

### 오픈소스의 성장과 구조적 한계

오픈소스 프로젝트는 대개 작고 단순한 형태로 시작해 점진적으로 확장된다. 사용자가 늘어나면 기능이 추가되고, 기존 사용자들을 위해 하위 호환성이 유지된다. 이러한 과정 속에서 프로젝트는 점점 거대한 구조로 성장한다. 이것이 오랫동안 오픈소스의 강점이었다. 누구나 기여할 수 있고, 축적된 개선이 자산이 되었다.

그러나 시간이 지나면 문제가 발생한다. 기반 인프라는 낡고, 구조적 부채는 쌓이며, 초기 설계의 한계가 드러난다. 프로젝트는 이미 거대한 배가 되어 방향을 틀기 어려워진다. 이를 해결하기 위해 두 가지 방식이 등장한다. 하나는 기존 코드를 기반으로 다른 길을 가는 포크(fork)이고, 다른 하나는 API 계약만 유지한 채 내부를 완전히 다시 구현하는 전면 재작성(total rewrite)이다. 예를 들어 Redpanda는 Kafka와 호환되지만 내부는 완전히 새로 작성된 사례다. 또한 S3 호환 API들이 널리 퍼진 것 역시 동일한 현상이다.

### 상업적 오픈소스의 인센티브 충돌

완전히 비영리적인 오픈소스 프로젝트라면 포크나 재작성은 자연스러운 흐름일 수 있다. 누군가 다른 방향으로 가더라도 본질적인 이해관계 충돌이 없다. 그러나 상업적 오픈소스는 다르다. 겉으로는 이타적 가치와 공동체 기여를 강조하지만, 동시에 기업으로서 생존과 수익이라는 현실적 인센티브를 갖고 있다.

많은 기업이 장기적 신뢰를 위해 단기적 이익을 포기하기도 한다. 하지만 완전히 이타적일 수는 없다. 명확한 API와 좋은 문서, 강력한 테스트는 사용자 신뢰를 높이지만, 동시에 경쟁자가 이를 기반으로 재구현하기 쉽게 만든다. 바로 이 지점에서 긴장이 발생한다.

### Cloudflare와 Next.js 사례

이 현상은 Cloudflare와 Next.js 사례에서 잘 드러난다. Next.js는 오랜 시간에 걸쳐 문서화, API 계약 정의, 테스트 스위트 구축에 막대한 투자를 해왔다. 이는 사용자 신뢰를 구축하는 핵심 자산이었다.

그러나 Cloudflare는 AI를 활용해 Next.js와 호환되는 대안을 빠르게 구축했다. 기존 사용자 기반이 없기 때문에 하위 호환성 부담도 없었다. 최신 API 표면만 지원하면 되었고, 내부 구현은 더 가볍고 현대적인 도구 위에 재구성할 수 있었다. 테스트 통과율 94%라는 수치는 문서 이상의 가치가 테스트에 있음을 보여준다. 말로 설명된 기능보다, 자동화된 검증 코드가 훨씬 더 강력한 재현 도구가 된 것이다.

### SQLite의 선택: 테스트는 비공개

흥미로운 대비 사례로 SQLite가 있다. SQLite는 핵심 엔진 코드는 오픈소스로 공개하지만, 방대한 테스트 스위트는 공개하지 않는다. 공식 문서에 따르면 테스트 코드는 수천만 줄에 달하며, 이는 핵심 소스 코드보다 훨씬 크다.

이 전략은 일부에게 오픈소스 정신에 어긋나는 것으로 보일 수 있다. 그러나 다른 관점에서는 이것이 바로 프로젝트의 지속 가능성을 지키는 해자다. AI 시대에는 테스트가 곧 동작 정의서이자 품질 보증 체계이기 때문이다. 이를 공개하면 복제 비용이 급격히 낮아질 수 있다.

### 새로운 해자: 테스트와 계약

AI 시대에 코드 자체는 점점 상품화되고 있다. 대신 진정한 자산은 다음 세 가지로 이동한다.

첫째, 명확하게 정의된 API 계약.
둘째, 동작을 정확히 검증하는 테스트 스위트.
셋째, 인터페이스 표면과 그 일관성.

과거에는 코드를 직접 작성하고 이해하는 능력이 경쟁력이었다. 이제는 코드가 계약을 얼마나 정확히 만족하는지가 핵심이다. 그리고 그 계약을 가장 엄격하게 표현하는 것이 바로 테스트다.

### 오픈소스의 미래에 대한 질문

결국 상업적 오픈소스는 선택의 기로에 서 있다. API와 테스트를 완전히 공개하여 생태계를 확장할 것인가, 아니면 일부를 해자로 남겨 지속 가능성을 확보할 것인가. AI가 복제 비용을 낮출수록 이 고민은 더욱 심화될 것이다.

이 글의 핵심 메시지는 단순하다.
AI 시대에는 코드가 아니라 테스트가 가장 강력한 자산이 될 수 있다. 그리고 상업적 오픈소스는 그 사실을 점점 더 의식하게 될 것이다.

# 26.03.18

## C# struct default constructor

C#의 **`struct`**(값 타입)와 **기본 생성자(Parameterless Constructor)** 동작 방식은 C# 버전(특히 C# 10.0 전후)에 따라 큰 차이가 있습니다. **필드 초기화와 `default` 키워드의 관계**를 정리해 드립니다.

⚠️ 개발자가 "필드에서 초기화했으니 절대 `null`이 아닐 거야"라고 확신하고 코드를 짰을 때 **`NullReferenceException`**이 발생할 수 있습니다.

```csharp
public struct MyData
{
    public object Protector = new(); // C# 10+ 가능
}

// ... 

MyData[] list = new MyData[5];
// list[0].Protector 는 null입니다! 생성자가 호출되지 않았기 때문입니다.

```

### "어떻게 생성하느냐"에 따라 다릅니다.

`public object Obj = new();`와 같이 필드에서 직접 초기화(Field Initializer)를 하더라도, **어떤 방식으로 구조체를 만드느냐**에 따라 결과가 달라집니다.

| 생성 방식            | 코드 예시                     | 결과 (`Obj` 멤버)                    |
| ---                  | ---                           | ---                                  |
| **`new MyStruct()`** | `var s = new MyStruct();`     | **`new object()`** (인스턴스 할당됨) |
| **`default` 키워드** | `var s = default(MyStruct);`  | **`null`**                           |
| **배열 할당**        | `var arr = new MyStruct[10];` | **`null`** (모든 요소)               |

### 왜 이런 차이가 발생하나요?

#### **A. C# 10.0 미만 (구형 규격)**

과거에는 `struct` 내부에서 필드를 직접 초기화하거나 매개변수가 없는 생성자를 정의하는 것 자체가 **불가능**했습니다. 모든 구조체는 강제로 모든 비트를 `0`으로 미는 `default` 동작만 수행했기 때문입니다.

#### **B. C# 10.0 이상 (현대적 규격)**

이제 구조체에서도 **명시적 기본 생성자**와 **필드 초기화**가 허용됩니다.

* `new MyStruct()`를 호출하면 C# 컴파일러는 사용자가 정의한 필드 초기화 로직(또는 기본 생성자)을 실행합니다. 따라서 이때는 `new object()`가 할당됩니다.
* 하지만 **`default`**나 **배열 할당**은 성능 최적화를 위해 메모리 영역을 단순히 `0`으로 초기화(Zero-init)합니다. 이 과정에서는 **생성자 로직이 호출되지 않으므로** 참조 타입인 `object`는 `null`이 됩니다.
